use eframe::egui::{FontDefinitions, Ui};

/// Displays the current font definition from the core app widget and displays the ui to detect any addition changes.
pub fn fonts_view(font_definitions: &mut FontDefinitions, ui: &mut Ui) {
    // font_definitions.
    ui.label("TODO");
}