use egui::{Style, Ui};

/// Displays the current font definition from the core app widget and displays the ui to detect any addition changes.
pub fn shape_view(_: &mut Style, ui: &mut Ui) {
    // font_definitions.
    ui.label("TODO: Create the UI for modifying shapes");
}
